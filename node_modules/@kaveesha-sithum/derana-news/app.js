const derananews = require('./index.js');

async function run() {
    try {
        const result = await derananews();
        console.log('Result:', result);
    } catch (error) {
        console.error('Error:', error.message);
    }
}

run();

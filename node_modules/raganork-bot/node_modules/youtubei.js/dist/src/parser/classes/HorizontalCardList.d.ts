import { YTNode } from '../helpers';
import SearchRefinementCard from './SearchRefinementCard';
import Button from './Button';
import MacroMarkersListItem from './MacroMarkersListItem';
declare class HorizontalCardList extends YTNode {
    static type: string;
    cards: import("../helpers").ObservedArray<SearchRefinementCard | MacroMarkersListItem>;
    header: YTNode | null;
    previous_button: Button | null;
    next_button: Button | null;
    constructor(data: any);
}
export default HorizontalCardList;
